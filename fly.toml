app = "bot-global-chat"
primary_region = "nrt"

[build]

[processes]
  bot = "node dist/bot.cjs"

[env]
  NODE_ENV = "production"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
  processes = ["bot"]
