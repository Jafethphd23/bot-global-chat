app = "bot-global-chat"
primary_region = "nrt"

[build]

# Use "app" as the main process name (standard on Fly.io)
[processes]
  app = "node dist/bot.cjs"

[env]
  NODE_ENV = "production"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
